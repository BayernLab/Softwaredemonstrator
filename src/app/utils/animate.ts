/**
 * An enum representing the different types of animations for entities.
 *
 * @enum {string}
 */
export enum AnimationKey {
  WALK = 'Walking',
  JUMP = 'Jump',
  IDLE = 'Idle',
  DEATH = 'Death',
  WAVE = 'Wave',
  YES = 'Yes',
  NO = 'No',
}

export default AnimationKey;
