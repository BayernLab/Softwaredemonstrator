/**
 * An enum representing the different types of ground blocks.
 *
 * @enum {string}
 */
export enum BlockType {
  mud = 'mud',
  ground = 'ground',
  earth = 'earth',
  water = 'water',
  ice = 'ice',
  lava = 'lava',
  snow = 'snow',
}

export default BlockType;
