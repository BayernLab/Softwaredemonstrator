/**
 * An enumeration of all possible entity types supported by the game.
 *
 * @enum {string}
 */
export enum EntityType {
  robot = 'robot',
  block = 'block',
  star = 'star',
  tree = 'tree',
  fence = 'fence',
  mover = 'mover',
}
export default EntityType;
